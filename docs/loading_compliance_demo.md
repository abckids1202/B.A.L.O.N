# Loading Compliance Demo

Use `LOADING_COMPLIANCE` mode. Replay fallback: `python scripts/demo_cv_replay.py --scenario LOADING_COMPLIANCE`.

The visual utilization is an estimate from package tracks crossing configured ROI/lines. It is labelled as an estimate and should be reconciled with WMS/vehicle capacity in production.
