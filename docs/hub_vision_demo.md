# Hub Vision Demo

Use `HUB_VISION` mode. Replay fallback: `python scripts/demo_cv_replay.py --scenario HUB_CONGESTION`.

The package detector plus ByteTrack-style tracking maps package centroids into fixed hub zones. Queue length, dwell, occupancy, congestion, and overflow forecast update the backend.
