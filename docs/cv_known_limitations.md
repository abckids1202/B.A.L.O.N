# CV Known Limitations

No `.pt`/`.onnx` weights were discovered in the imported YOLO folders, so live inference reports model weights missing until training/export is completed. Replay mode is explicit and uses the same backend event path.
