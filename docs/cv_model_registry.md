# CV Model Registry

Current discovered package asset: `Package and label detection.v4i.yolov11`, classes `label`, `package`.

Current discovered damage asset: `Parcel Damage Detection.v2-roboflow-instant-2--eval-.yolov11`, classes `crushed`, `dirt`, `puncture`, `tear`, `torn`, `wet`.

No `.pt` or `.onnx` weights are currently discovered. Train/export weights into `models/cv/package_detector/v1/best.pt` and `models/cv/damage_detector/v1/best.pt` before claiming live YOLO inference.
